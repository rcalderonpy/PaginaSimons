<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#" id="brand">{{ titulo }}</a>
        </div>
        
        <!-- BOTONES -->
        <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1">
            {#<ul class="nav navbar-nav nav-pills pull-right" style="vertical-align: middle">#}
            {% if botones != null %}
                {% for boton in botones %}
                    {% if app.request.attributes.get('_route') ends with 'edit' or app.request.attributes.get('_route') ends with 'show' %}
                            <a href="{{ path(boton.ruta, {'id_cliente':id_cliente, 'mes':mes, 'ano':ano, 'id_venta':id_venta})  }}" class="btn btn-default navbar-btn">{{ boton.texto }} </a>
                    {% else  %}
                        {% if id_cliente is not null and mes is null %}
                            <a href="{{ path(boton.ruta, {'id_cliente':id_cliente}) }}" class="btn btn-default navbar-btn">{{ boton.texto }} </a>
                        {% else %}
                            <a href="{{ path(boton.ruta, {'id_cliente':id_cliente, 'mes':mes, 'ano':ano})  }}" class="btn btn-default navbar-btn">{{ boton.texto }} </a>
                        {% endif %}
                    {% endif %}
                {% endfor %}
            {% endif %}
            {#</ul>#}
            <span class="sr-only">(current)</span>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>