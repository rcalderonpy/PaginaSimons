<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('venta_index') }}">{{ titulo }}</a>
        </div>
        
        <!-- BOTONES -->
        <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1">
            {#<ul class="nav navbar-nav nav-pills pull-right" style="vertical-align: middle">#}
                {% for boton in botones %}
                    {% if boton|length>2 %}
                        {#<li role="presentation" class="btn btn-default navbar-btn">#}
                            <a href="{{ path(boton.ruta, {id:boton.id})  }}" class="btn btn-default navbar-btn">{{ boton.texto }} </a>
                        {#</li>#}
                    {% else %}
                        {#<li role="presentation" class="btn btn-default navbar-btn">#}
                            <a href="{{ path(boton.ruta)  }}" class="btn btn-default navbar-btn">{{ boton.texto }} </a>
                        {#</li>#}
                    {% endif %}
                {% endfor %}
            {#</ul>#}
            <span class="sr-only">(current)</span>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>